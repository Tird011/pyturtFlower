def binPot(num, target):
  for num in target:
    if num == 0:
      return num

return -1

def __init__():
  num[] = {1,2,3,4,5,}
  target = 4
  results = binPot(num,target)
  if results == -1:
    print(results)
  else:
    print("its not here")
